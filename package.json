{
	"name": "@cora-ui/monorepo",
	"version": "1.0.12",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"overrides": {
		"react": "^19.0.0",
		"react-dom": "^19.0.0"
	},
	"scripts": {
		"preinstall": "npx only-allow npm",
		"build": "npm run build --workspaces --if-present",
		"version:bump": "npm version patch && npm version patch --workspaces --no-git-tag-version",
		"release": "npm run build && npm run version:bump && npm publish --workspace=@cora-ui/react && git push origin main --tags"
	},
	"packageManager": "npm@11.8.0",
	"engines": {
		"node": ">=22.0.0",
		"npm": ">=11.0.0"
	}
}
