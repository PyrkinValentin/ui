import { TabsRoot, TabsList, TabsTab, TabsIndicator, TabsPanel } from "./tabs"

export const Tabs = {
	Root: TabsRoot,
	List: TabsList,
	Tab: TabsTab,
	Indicator: TabsIndicator,
	Panel: TabsPanel,
} as const