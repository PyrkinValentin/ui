import {
	AlertRoot,
	AlertIndicator,
	AlertContent,
	AlertTitle,
	AlertDescription,
} from "./alert"

export const Alert = {
	Root: AlertRoot,
	Indicator: AlertIndicator,
	Content: AlertContent,
	Title: AlertTitle,
	Description: AlertDescription,
}