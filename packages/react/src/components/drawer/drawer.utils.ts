import { Drawer } from "@base-ui/react/drawer"

/**
 * Creates a new handle to connect a Drawer.Root with detached Drawer.Trigger components.
 */
export const createDrawerHandle = Drawer.createHandle
